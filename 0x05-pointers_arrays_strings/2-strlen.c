#include "main.h"
#include<string.h>
/**
 *_strlen - Returs the length of string
 *@s: Variable to be checked
 *Return: Returns the length of the string
 */
int _strlen(char *s)
{
	return (strlen(*s));
}
