/**
 * main- Prints all the base 16 numbers
 * using the putchar function only
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 16; n++)
	{
		putchar(n);
	}
	putchar('\n')
	return (0);
}
