#include<ctype.h>
#include "main.h"
/**
 *_abs - Prints the absolute value of a number
 *@n: Number to be checked
 *
 *return: always true (success)
 */
int _abs(int)
{
	if (n < 0 )
	{
		return (-n);
	}
	else
		return (n);
}

